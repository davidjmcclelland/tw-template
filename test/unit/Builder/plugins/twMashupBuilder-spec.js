describe('twMashupBuilder', () => {
    describe('TW.IDE.calculateGadgetWidth', () => {
        it('should calculate correctly based on rows and columns', () => {
            let actual = 240;
            expect(actual).toBe(240);
        });
    });
});
